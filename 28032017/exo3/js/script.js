// Créer une variable de type string : chaine de caractéres
var myString = 'Je suis un texte';
console.log(myString);

// Créer une variable de type NUMBER (INTEGER)
var myNumber = 159;
console.log(myNumber);

// Créer un variable de type BOOLEAN : true/false
var myBoolean = true;
console.log(myBoolean);