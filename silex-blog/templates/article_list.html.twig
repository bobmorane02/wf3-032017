    {% for article in articles %}
        <h3>{{ article.title }} <small>{{ article.categoryName }}</small></h3>
        <p><b>{{ article.short_content }}</b></p>
        <p>{{ article.content }}</p>
        {% if not loop.last %}
            <hr>
        {% endif %}
    {% endfor %}