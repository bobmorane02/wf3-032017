// Attendre le chargement du DOM

$(document).ready(function(){
    // Le code de la page est à intégrer dans la fonction de callback
    console.log('Le DOM est chargé');
});

console.log('DOM n\'est pas chargé');